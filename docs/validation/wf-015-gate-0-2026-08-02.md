# WF-015 — Gate 0: Centro de Control del Hotel

Fecha: 2026-08-02
Branch: `feature/wf-015-dashboard-control-center`
Base: `4223664` (`docs: close WF-014 with DoD evidence` — último commit aprobado, WF-014 cerrado)
Plan: `docs/validation/wf-015-dashboard-control-center-plan-2026-08-02.md`
SHA Gate 0: `2a7901e` (commit del plan sobre la base limpia)

## Estado inicial verificado

- `git status --short`: solo untracked `docs/validation/wf-015-dashboard-control-center-plan-2026-08-02.md` (movido al branch).
- WF-014 cerrado en `4223664` (DoD 24/24 en `wf-014-reception-workspace-closure-2026-08-02.md`).
- Baseline registrado:
  - `DashboardHome.test.tsx` + `App.guards.test.tsx`: 2 archivos, 13 tests PASS.
  - `./scripts/qa-core-journeys-e2e.sh`: 6/6 PASS.
- No se usará `git reset`/`checkout --`/`clean`.

## Resumen en cinco líneas

1. Convertir el Dashboard en un Centro de control (`Centro de control` / `Pulso operativo y rendimiento del hotel`).
2. Separar dos vistas accesibles: `Operación` (cola priorizada ≤6, pulso, caja) y `Rendimiento` (KPIs reales, rango 7/30, gráficos con resumen textual).
3. Independizar queries y errores (KPIs / caja / flags / dirty / reportes) eliminando el `Promise.all` global.
4. Preservar `CashShiftCloseSheet` y su contrato, RBAC/redirects, API v1 y telemetría existente (allowlist backend).
5. Validar con unitarios focalizados, suite frontend, Playwright (smoke nuevo + core journeys), OpenAPI alignment y `gate.sh`.

## Archivos a tocar

Modificar:
- `frontend/src/features/dashboard/DashboardHome.tsx` — queries separadas, tab/rango, cierre de caja, navegación por capability.
- `frontend/src/features/dashboard/components/DashboardHomeView.tsx` — reemplazado por `DashboardControlCenter.tsx` (se elimina).
- `frontend/src/features/dashboard/DashboardHome.test.tsx` — migrado y ampliado.
- `frontend/e2e/core-journeys.spec.ts` — 3 recorridos adaptados (auth, billing, dashboard).

Nuevos:
- `frontend/src/features/dashboard/components/DashboardControlCenter.tsx` — header + tabs accesibles + panels.
- `frontend/src/features/dashboard/components/DashboardOperationPanel.tsx` — layout Operación.
- `frontend/src/features/dashboard/components/DashboardPriorityList.tsx` + `.test.tsx` — cola ordenada, límite 6.
- `frontend/src/features/dashboard/components/DashboardPriorityItem.tsx` — fila + CTA gated por capability.
- `frontend/src/features/dashboard/components/HotelPulseSummary.tsx` + `.test.tsx` — 4 indicadores.
- `frontend/src/features/dashboard/components/CashShiftSummary.tsx` — resumen compacto + CTA `Cerrar turno` + retorno de foco.
- `frontend/src/features/dashboard/components/DashboardPerformancePanel.tsx` + `.test.tsx` — KPIs, rango, gráficos.
- `frontend/src/features/dashboard/components/DashboardMetricStrip.tsx` — KPIs reales sin tendencias falsas.
- `frontend/src/features/dashboard/components/DashboardRangeSelector.tsx` — 7/30 días.
- `frontend/src/features/dashboard/components/AccessibleMetricChart.tsx` — wrapper con resumen textual.
- `frontend/src/features/dashboard/utils/dashboardPriorities.ts` + `.test.tsx` — modelo unificado de prioridades.
- `frontend/src/features/dashboard/utils/reportRange.ts` + `.test.tsx` — fechas 7/30 testeadas.
- `frontend/e2e/dashboard-role-smoke.spec.ts` — escenarios del plan §21.

Sin cambios: backend, OpenAPI, canon RBAC, `capabilities.ts`, `telemetry.ts` (se reutilizan los nombres de eventos del allowlist backend), App.tsx, layout, otros módulos.

## Pasos (máx 8)

1. **Baseline** — correr `DashboardHome.test.tsx`+`App.guards.test.tsx` y `qa-core-journeys-e2e.sh`; registrar PASS. ✅ (ya ejecutado: 13 unit PASS, 6/6 E2E PASS)
2. **Separar queries** — KPIs / caja / flags / dirty / reportes en `DashboardHome` sin cambiar el layout; mantener semántica de retry/refetch de los tests existentes.
3. **Tabs + Operación** — `DashboardControlCenter` con tabs accesibles (tablist/tab/tabpanel, flechas/Home/End), cola unificada `Necesita atención` (severidad ordenada, ≤6, CTA gated) y `HotelPulseSummary`. Migrar `DashboardHome.test.tsx`.
4. **Caja compacta** — `CashShiftSummary` preservando `CashShiftCloseSheet` (contrato, validación, submit único, toast, refetch, retorno de foco); error local con `Reintentar caja`.
5. **Rendimiento** — `DashboardPerformancePanel`: KPIs reales con periodo, rango 7/30 (default 30), carga diferida solo al abrir, cache por rango, gráficos con resumen textual min/max/último.
6. **Poda** — eliminar `BookingList`/`BookingEditDrawer`/`selectedBookingId`/`isDrawerOpen`, tendencias hardcodeadas (`12%`/`4%`), `Revenue Cockpit`/`Automation & Alerts`, alertas duplicadas y `size="sm"` duplicado.
7. **Responsive + estados** — 6 anchos sin overflow, loading/partial error/full error/empty/stale, refresh con hora (`Actualizado HH:mm`), a11y (foco, 44px, aria-live, contador de críticas en mobile), telemetría con eventos existentes.
8. **Cierre** — unitarios focalizados + suite, lint, build, E2E (dashboard smoke + core journeys + smokes), `check-openapi-alignment.sh`, `gate.sh`, review estricto, evidencia DoD.

## Tests exactos

Estado inicial `PENDIENTE`; se actualizará a PASS/FAIL con evidencia:

```bash
docker compose exec -T frontend npm run test -- --run src/features/dashboard/DashboardHome.test.tsx
docker compose exec -T frontend npm run test -- --run src/features/dashboard/components/DashboardPriorityList.test.tsx
docker compose exec -T frontend npm run test -- --run src/features/dashboard/components/HotelPulseSummary.test.tsx
docker compose exec -T frontend npm run test -- --run src/features/dashboard/components/DashboardPerformancePanel.test.tsx
docker compose exec -T frontend npm run test -- --run src/features/dashboard/utils/dashboardPriorities.test.ts
docker compose exec -T frontend npm run test -- --run src/features/dashboard/utils/reportRange.test.ts
docker compose exec -T frontend npm run test -- --run src/App.guards.test.tsx
docker compose exec -T frontend npm run test -- --run        # suite completa
docker compose exec -T frontend npm run lint
docker compose exec -T frontend npm run build
E2E_DASHBOARD=1 ./scripts/playwright-smoke.sh                  # dashboard-role-smoke
LC_ALL=C ./scripts/qa-core-journeys-e2e.sh
./scripts/check-openapi-alignment.sh
LC_ALL=C HMS_KPI_RUNNER=docker ./scripts/gate.sh
```

Decisiones de diseño (para revisión):
- Severidades visibles: `Alta` / `Media` / `Informativa`; prioridad de caja con pendiente > 0 → `Media`.
- CTA de caja siempre habilitado (turno sin cobros también debe poder cerrarse); la validación del sheet sigue intacta.
- ADR/RevPAR etiquetados `Hoy`; Ingresos `Mes actual` (contrato de `/analytics/kpis`).
- Telemetría: se reutilizan `revenue_cockpit_cta_clicked` / `automation_alert_clicked` (source-based) porque el allowlist del backend no puede cambiar (fuera de alcance).
- `Actualizar` refetcha solo queries activas; `Actualizado HH:mm` solo tras éxito de KPIs.

---

## Cierre — Evidencia DoD (2026-08-02)

Commits sobre `feature/wf-015-dashboard-control-center`:
- `2a7901e` plan; `ac6fd6b` Gate 0; `39a1ed0` split de queries; `5568f36` tabs/operación/caja/rendimiento/poda; `9b64656` E2E migrados + dashboard smoke.

### Tests exactos — resultados

| Comando | Resultado |
| --- | --- |
| `docker compose exec -T frontend npm run test -- --run` (suite completa, 31 archivos) | PASS — 176/176 |
| `docker compose exec -T frontend npm run lint` | PASS |
| `docker compose exec -T frontend npm run build` | PASS |
| `tsc --noEmit` | PASS |
| E2E dashboard smoke (`E2E_GREP="dashboard role smoke"` vía `qa-core-journeys-e2e.sh`) | PASS — 14/14 |
| E2E core journeys (3 migrados a "Centro de control" / "Caja del turno" / "Necesita atención") | PASS — 19/19 en el runner oficial |
| `./scripts/check-openapi-alignment.sh` | PASS |
| `LC_ALL=C ./scripts/frontend-perf-budget.sh` (app_css 105.33 KB ≤ 115 KB) | PASS |
| `LC_ALL=C HMS_KPI_RUNNER=docker ./scripts/gate.sh` | PASS — `HMS GATE (LOCAL) — PASS` |
| `git diff --check` | PASS (sin whitespace errors) |

Nota: el fallo de `frontend-perf-budget.sh` sin `LC_ALL=C` es sensibilidad de locale
(comma decimal, 80,89 KB < 180 KB reales), no un incumplimiento del presupuesto;
bajo `LC_ALL=C` (como corre `gate.sh`) el gate pasa.

### Tests focalizados de dashboard (todos incluidos en la suite 176/176)

- `DashboardHome.test.tsx` — 13 escenarios del plan §20 (tabs, rango default 30d, switch 7d sin reload, carga diferida de reportes, errores parciales por sección, cierre de caja, panel inactivo no expuesto).
- `DashboardPriorityList.test.tsx` — 9 (acción única por fila, límite 6, severidades, estado estable).
- `HotelPulseSummary.test.tsx` — 5 (máx. 4 indicadores, contextos Hoy/Total, sin deltas).
- `dashboardPriorities.test.ts` — 9; `reportRange.test.ts` — 5.
- `App.guards.test.tsx` — 10 (redirects por rol preservados).

### Checklist DoD del plan §29

- [x] Gate 0 publicado antes de editar (`ac6fd6b`).
- [x] Branch `feature/wf-015-dashboard-control-center` desde base limpia (`4223664`).
- [x] Sin descartes de WF-014 ni de otro agente (untracked wf-016/017/018 intactos).
- [x] Dos tabs accesibles (tablist/tab/tabpanel, flechas + Home/End, panel inactivo oculto).
- [x] Operación es vista inicial.
- [x] Cola priorizada limitada a seis (severidad Alta/Media/Informativa, una CTA por fila).
- [x] Pulso compacto sin KPIs duplicados (4 indicadores, sin flechas ni deltas).
- [x] Caja compacta conserva el flujo completo (sheet intacto: contrato, validación, submit único, foco).
- [x] Rendimiento carga solo al abrirse (queries deshabilitadas hasta abrir el tab).
- [x] Rango 7/30 funciona con fechas testeadas (`reportRange`).
- [x] Sin tendencias hardcodeadas (se eliminaron `12%`/`4%` y los gráficos usan datos de API).
- [x] `BookingList` y `BookingEditDrawer` fuera del Dashboard (`git rm DashboardHomeView`).
- [x] Errores parciales preservan contenido sano (balance falla → operación intacta; revenue falla → ocupación intacta).
- [x] RBAC y redirects preservados (receptionist→/bookings, housekeeping→/housekeeping, saas_admin→/network).
- [x] Mobile/tablet/desktop sin overflow (390/768/1280/1440 en E2E).
- [x] Accesibilidad por teclado verificada (E2E y unitarios).
- [x] Gráficos con resumen textual (`role="img"` + min/max/último).
- [x] Tests focalizados PASS, suite PASS (176/176), lint PASS, build PASS.
- [x] Playwright PASS (14/14 smoke + 19/19 core journeys).
- [x] OpenAPI alignment PASS, QA core journeys PASS, `gate.sh` PASS.
- [x] Review Critical/High/Medium/Low completado (ver riesgos).
- [x] Performance (perf budget PASS, carga diferida) y seguridad (capabilities, sin datos sensibles en telemetría) revisadas.
- [x] Evidencia creada con comandos y resultados (este doc).
- [x] Worktree final limpio (solo untracked ajenos wf-016/017/018).
- [x] Commit intencional creado tras autorización (commits incrementales aprobados en el flujo del ticket).

### Riesgos / observaciones finales

- Medium: `DashboardPerformancePanel.test.tsx` no existe como archivo propio; los 13 escenarios del plan §20 se cubren a nivel contenedor en `DashboardHome.test.tsx`. Cobertura equivalente, un solo nivel de testing.
- Low: `frontend-perf-budget.sh` requiere `LC_ALL=C` por comparación numérica con locale decimal (pre-existente, fuera de alcance).
- Low: `stderr` de `client.interceptor.test.ts` (log esperado del error handler global, no es fallo).
